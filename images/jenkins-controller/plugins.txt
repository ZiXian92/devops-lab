# Unpinned = latest compatible with the controller version at build time.
configuration-as-code
matrix-auth
# Jenkins Templating Engine and what multibranch pipelines need around it.
workflow-aggregator
workflow-multibranch
branch-api
cloudbees-folder
git
hashicorp-vault-plugin
secret-guard
# JCasC's `jobs:` root key (defines the vault-secret-demo pipeline job).
job-dsl
